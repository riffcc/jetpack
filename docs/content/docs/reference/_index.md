---
title: Reference
weight: 8
cascade:
  type: docs
sidebar:
  open: true
---

This section is **auto-generated** from the source (`src/registry/list.rs` and `src/cli/parser.rs`). Don't edit these pages by hand — run `jetpack gen-reference` to regenerate, and edit [`reference.json`](https://github.com/riffcc/jetpack/blob/main/docs/reference.json) to add descriptions, examples, or parameter docs.

> **Notes:**
>
> Module tags are the lowercased Task enum variant in src/registry/list.rs. `!yum` and `!dnf` are two tags for the same YumDnfTask implementation. These pages are regenerated by `jetpack gen-reference` — edit this file, not the pages under docs/content/docs/reference/.
>

## Modules

### access

| Module | Description |
|---|---|
| [`!group`](modules/group) | — |
| [`!user`](modules/user) | — |

### commands

| Module | Description |
|---|---|
| [`!command`](modules/command) | Run a command on the target directly, without a shell. Safer than `!shell` (no shell injection) but no pipes, redirection, or builtins. See [shell vs command](../../../usage/shell-vs-command/). |
| [`!external`](modules/external) | — |
| [`!shell`](modules/shell) | Run a command through a real shell (default `/bin/bash`). Use when you need pipes, redirection, or shell builtins. |

### control

| Module | Description |
|---|---|
| [`!assert`](modules/assert) | Assert that an expression is true; fail the run otherwise. |
| [`!debug`](modules/debug) | — |
| [`!echo`](modules/echo) | Print a message. Useful for debugging playbooks; the message is templated. |
| [`!facts`](modules/facts) | Gather system facts (OS, architecture, distribution) about the target and expose them as `jet_*` variables for subsequent tasks. Must run before any `jet_*` variable is used. |
| [`!fail`](modules/fail) | — |
| [`!self_locate`](modules/self_locate) | — |
| [`!set`](modules/set) | — |
| [`!wait_for_host`](modules/wait_for_host) | — |
| [`!wait_for_others`](modules/wait_for_others) | — |

### files

| Module | Description |
|---|---|
| [`!copy`](modules/copy) | — |
| [`!directory`](modules/directory) | — |
| [`!download`](modules/download) | — |
| [`!fetch`](modules/fetch) | — |
| [`!file`](modules/file) | Manage the state of a file, directory entry, or symlink on the target. Set attributes (mode, owner, group) or remove the path. |
| [`!git`](modules/git) | — |
| [`!move`](modules/move) | — |
| [`!stat`](modules/stat) | — |
| [`!template`](modules/template) | — |
| [`!unpack`](modules/unpack) | — |

### integrations

| Module | Description |
|---|---|
| [`!github_release`](modules/github_release) | — |

### inventory

| Module | Description |
|---|---|
| [`!instantiate`](modules/instantiate) | — |

### packages

| Module | Description |
|---|---|
| [`!apt`](modules/apt) | Manage packages with apt on Debian/Ubuntu. |
| [`!dnf`](modules/dnf) | Manage packages with yum/dnf on RHEL-family hosts. `!yum` and `!dnf` are the same module (YumDnfTask). |
| [`!homebrew`](modules/homebrew) | — |
| [`!pacman`](modules/pacman) | — |
| [`!yum`](modules/yum) | Manage packages with yum/dnf on RHEL-family hosts. `!yum` and `!dnf` are the same module (YumDnfTask). |
| [`!zypper`](modules/zypper) | — |

### proxmox

| Module | Description |
|---|---|
| [`!proxmox_lxc`](modules/proxmox_lxc) | — |
| [`!proxmox_migrate`](modules/proxmox_migrate) | — |
| [`!proxmox_node`](modules/proxmox_node) | — |

### services

| Module | Description |
|---|---|
| [`!sd_service`](modules/sd_service) | — |

## Command line

See [CLI reference](cli) for modes and flags.
