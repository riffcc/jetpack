name: Deploy Docs to GitHub Pages

# Builds the Hugo/Hextra site under docs/ and publishes it to GitHub Pages,
# served at https://jetpack.dragonfly.computer/. baseURL is set in docs/hugo.yaml
# and docs/static/CNAME carries the custom domain. Repo prerequisites:
#   - Settings → Pages → Source = "GitHub Actions"
#   - Settings → Pages → Custom domain = jetpack.dragonfly.computer (the
#     static/CNAME file sets this automatically on first deploy)
#   - DNS: CNAME jetpack.dragonfly.computer -> riffcc.github.io

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.147.7
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # enableGitInfo needs full history

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: true

      - name: Build Hugo site
        working-directory: docs
        run: hugo --gc --minify --enableGitInfo

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
